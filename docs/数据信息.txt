1. 用药信息表.xlsx
业务定位：牛只用药历史记录表，核心用于追溯单头牛只的全周期用药情况，是养殖健康管理的核心业务表，通过资产 ID 与牛只基础信息强关联。
核心字段：自增 ID、租户 ID、资产 ID、智能耳标编号、用药日期、用药名称、用药方式 (1 内服 / 2 外用 / 3 注射)、用药说明、备注、创建人 / 创建时间、修改人 / 修改时间等
数据规模：29 行 × 18 列，共 29 条牛只用药记录
2. 药品信息表.xlsx
业务定位：药品基础信息主表，记录养殖场可用药品的基础属性、分类、规格等标准化信息，是用药信息表的基础字典表。
核心字段：自增 ID、租户 ID、药品名称、药品编码、药品类型、规格、单位、生产厂家、有效期、备注、创建 / 修改人及时间等
数据规模：34 行 × 17 列，共 34 条药品基础数据
3. 喂养精料配方项表.xlsx
业务定位：精料配方的明细构成表，记录每个精料配方的具体原料、占比、用量等明细信息，是精料配方表的子表，用于还原饲料配方的精准成分。
核心字段：自增 ID、租户 ID、配方 ID、原料名称、原料编码、原料占比、原料用量、单位、排序、备注、创建 / 修改人及时间等
数据规模：完整记录了所有精料配方的原料明细，可通过配方 ID 关联精料配方主表
4. 喂养记录饲料表.xlsx
业务定位：喂养记录的饲料消耗明细表，记录单次喂养任务中各类饲料（精料、草料、酒糟等）的实际投放量、消耗情况，是喂养记录表的子表。
核心字段：自增 ID、租户 ID、喂养记录 ID、饲料类型、饲料名称、投放量、单位、剩余量、损耗量、备注、创建 / 修改人及时间等
数据规模：与喂养记录表强关联，可通过喂养记录 ID 追溯单次喂养的饲料消耗明细
5. 喂养记录明细项表.xlsx
业务定位：喂养记录的执行明细子表，记录单次喂养任务中，每个喂养分组的实际执行情况、投喂时间、投喂人等明细数据，是喂养记录表的核心明细支撑表。
核心字段：自增 ID、租户 ID、喂养记录 ID、喂养分组 ID、投喂日期、投喂时间、实际投喂量、投喂人、执行状态、备注、创建 / 修改人及时间等
数据规模：完整覆盖所有喂养任务的分组执行明细，可通过喂养记录 ID 关联主表
6. 喂养分组资产表.xlsx
业务定位：牛只与喂养分组的关联映射表，核心用于标记每头牛只所属的喂养分组，是喂养业务与牛只资产的核心关联表，实现 “牛只 - 分组 - 喂养记录” 的全链路打通。
核心字段：自增 ID、租户 ID、喂养分组 ID、资产 ID、入组日期、出组日期、当前状态、备注、创建 / 修改人及时间等
数据规模：记录了所有牛只的分组归属信息，可通过资产 ID 关联牛只基础信息，通过喂养分组 ID 关联喂养分组表
7. 资产基础表.xlsx
业务定位：牛只资产的核心基础信息主表，是整个养殖系统的核心主数据，记录每头牛只的出生、入栏、基础属性等全生命周期基础信息。
核心字段：自增 ID、租户 ID、资产 ID、智能耳标编号、牛只品种、性别、出生日期、入栏日期、初始体重、当前状态、备注、创建 / 修改人及时间等
数据规模：覆盖所有牛只的基础档案信息，是关联成长、用药、防疫、喂养等所有业务表的核心主键表
8. 喂养分组表 —.xlsx
业务定位：喂养分组的基础信息主表，记录养殖场所有喂养分组的分组规则、分组类型、适用阶段、负责人等基础信息，是喂养业务的核心字典表。
核心字段：自增 ID、租户 ID、喂养分组 ID、分组名称、分组类型、适用牛只阶段、分组负责人、创建日期、备注、创建 / 修改人及时间等
数据规模：记录了养殖场所有喂养分组的基础配置信息，可通过喂养分组 ID 关联喂养分组资产表、喂养记录表
9. 成长信息表.xlsx
业务定位：牛只生长发育核心数据表，记录每头牛只不同时间节点的体重、体尺、月龄等生长数据，是本项目预测牛只日增重的核心目标表。
核心字段：自增 ID、租户 ID、资产 ID、智能耳标编号、测量日期、体重、体高、体长、胸围、月龄、日增重、备注、创建 / 修改人及时间等
数据规模：完整记录了牛只全周期的生长测量数据，是构建生长预测模型的核心数据源
10. 死亡信息表.xlsx
业务定位：牛只死亡信息记录表，记录养殖场死亡牛只的死亡时间、死亡原因、处置方式等信息，用于牛只生命周期的状态闭环管理。
核心字段：自增 ID、租户 ID、资产 ID、智能耳标编号、死亡日期、死亡原因、处置方式、处置人、备注、创建 / 修改人及时间等
数据规模：记录了所有死亡牛只的明细信息，可通过资产 ID 关联牛只基础信息，标记牛只的最终状态
11. 精料配方表 —.xlsx
业务定位：精料配方的主表，记录养殖场所有精料配方的基础信息、适用阶段、营养指标、创建人等核心信息，是喂养业务的核心配方字典表。
核心字段：自增 ID、租户 ID、配方 ID、配方名称、适用牛只阶段、配方总营养值、创建人、创建日期、备注、创建 / 修改人及时间等
数据规模：记录了所有精料配方的基础信息，可通过配方 ID 关联喂养精料配方项表，还原配方的完整成分
12. 防疫信息表.xlsx
业务定位：牛只防疫接种记录表，记录每头牛只的疫苗接种、防疫措施执行情况，是牛只健康管理的核心业务表，用于追溯牛只的防疫全周期。
核心字段：自增 ID、租户 ID、资产 ID、智能耳标编号、防疫日期、防疫项目、疫苗名称、接种方式、接种人、备注、创建 / 修改人及时间等
数据规模：记录了所有牛只的防疫执行明细，可通过资产 ID 关联牛只基础信息，量化防疫对牛只生长的影响
13. 出售信息表.xlsx
业务定位：牛只出售信息记录表，记录养殖场出栏牛只的出售时间、收购方、出售重量、出售价格等核心经营数据，是养殖业务的经营闭环表。
核心字段：自增 ID、租户 ID、资产 ID、智能耳标编号、出售日期、收购方、出售重量、出售单价、总金额、经办人、备注、创建 / 修改人及时间等
数据规模：记录了所有出售牛只的经营明细，可通过资产 ID 关联牛只基础信息，标记牛只的最终状态
14. 喂养记录明细表.xlsx
业务定位：喂养记录的全量明细子表，是喂养记录表的核心明细支撑表，记录单次喂养任务中，每个牛只 / 分组的实际投喂明细、营养摄入等精细化数据。
核心字段：自增 ID、租户 ID、喂养记录 ID、资产 ID / 喂养分组 ID、投喂日期、实际投喂量、营养摄入值、投喂人、执行状态、备注、创建 / 修改人及时间等
数据规模：覆盖所有喂养任务的精细化执行明细，可通过喂养记录 ID 关联喂养记录表，实现喂养全链路追溯
15. 喂养记录表.xlsx
业务定位：喂养业务的核心主表，记录养殖场所有喂养任务的核心信息，包括喂养分组、配方、喂养周期、总投喂量、执行状态等，是喂养业务的总控表。
核心字段：代理主键、租户 ID、喂养分组 ID、喂养分组名称、精料配方名称、喂养开始 / 结束时间、精料 / 草料 / 酒糟喂养总量、资产天、执行状态、成本计算、创建 / 修改人及时间等
数据规模：记录了所有喂养任务的核心汇总信息，共 25 列，可通过喂养记录 ID 关联喂养记录明细项表、喂养记录饲料表，通过喂养分组 ID 关联喂养分组表


2657 头牛（全量）
  ├── 513 头有生长记录（19.3%）  → 可用于日增重分析、牛犊选择聚类
  │     └── 106 头有饲喂记录（4.0%） → 可用于成本效益分析
  ├── 558 头有引入渠道名称（21.0%）→ 渠道对比（可能与生长记录有交集）
  ├── 1017 头已出售（38.3%）→ 有出栏数据
  └── 2144 头无生长记录（80.7%）→ 仅能做存栏画像